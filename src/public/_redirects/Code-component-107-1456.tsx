# Netlify Redirects for SPA (Single Page Application)
# This file ensures that all routes are handled by the React app

# Redirect all routes to index.html (SPA fallback)
/*    /index.html   200

# Force HTTPS (optional, recommended for production)
# http://:splat    https://:splat    301!

# Redirect www to non-www (optional)
# https://www.parcalamamakinesi.com/*    https://parcalamamakinesi.com/:splat    301!
